# BUET E-Council

## Getting Started

### Prerequisites
- Docker
- Docker Compose

### Development

To start the application:
```bash
docker compose up --build
```

To restart the application and remove volumes:
```bash
docker compose down -v; docker compose up --build
```
